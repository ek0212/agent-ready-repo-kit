# Claude Code Project Instructions

@AGENTS.md

## Claude Code Notes

- Treat `AGENTS.md` as always-loaded constraints, not a full repo summary.
- Use `.claude/rules/` for path-specific constraints.
- Use `.claude/skills/` for optional procedures and reference material.
- Inspect the current code and docs before relying on any summary.
